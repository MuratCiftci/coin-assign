/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['assets.coingecko.com', "www.coinkolik.com", "www.bitcoinsistemi.com"],
  },
}

module.exports = nextConfig
